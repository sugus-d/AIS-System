appId: edu.cuhk.ais.local
productName: AIS
directories:
  app: .release/app
  output: release
files:
  - "**/*"
extraResources:
  - from: .release/algorithm-runtime
    to: algorithm-runtime
  - from: .release/annotation-runtime
    to: annotation-runtime
  - from: .release/migrations
    to: migrations
  - from: .release/deployment
    to: deployment
  - from: .release/manifest.json
    to: manifest.json
asar: true
asarUnpack:
  - node_modules/better-sqlite3/**
  - node_modules/@prisma/**
  - node_modules/@prisma/adapter-better-sqlite3/**
# 原生模块在 release 暂存（.release/app）内已是按 Electron ABI 编译好的；
# electron-builder 的自动重建会误解析到 pnpm store（只读）导致 EPERM，故关闭。
npmRebuild: false
compression: maximum
win:
  icon: build/icon.png
  target:
    - target: nsis
      arch:
        - x64
  artifactName: AIS-${version}-Setup.exe
  signAndEditExecutable: true
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  allowElevation: false
  deleteAppDataOnUninstall: false
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: AIS
